---
export interface Props {
  title: string;
  subTitle: string;
  firstChild: boolean;
}
const { title, subTitle, firstChild } = Astro.props;
---

<div class="flex h-20">
  <div
    class={`w-[2px] bg-zinc-200/10 flex justify-center ${
      firstChild ? "items-start h-1/2 mb-0 mt-auto" : "items-center h-full"
    }`}
  >
    <div class="w-2 h-2 rounded-full bg-yellow-500"></div>
  </div>

  <!-- <div
    class={`relative w-[2px] ${firstChild ? "h-20" : "h-20"} bg-zinc-200/10`}
  >
    <div
      class={`absolute top-0 bottom-0 left-0 right-0 m-auto h-1 rounded-full ring-4 ring-yellow-500 w-1 bg-yellow-500`}
    >
    </div>
  </div> -->
  <div class="my-auto">
    <div class="ml-6 text-yellow-500">{title}</div>
    <div class="ml-6 text-zinc-400">{subTitle}</div>
  </div>
</div>
